const link = document.getElementById("ForgetPassword");

// Adicionando um evento de clique ao link
link.addEventListener("click", () => {
  // Redirecionando para o site do Google
  alert('Seu usuário é:' + '\nSua senha é: ');
});
